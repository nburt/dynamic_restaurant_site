<!DOCTYPE html>
<html>
<head>
  <link href='//fonts.googleapis.com/css?family=Imprima' rel='stylesheet' type='text/css'>
  <link href="/css/normalize.min.css" rel="stylesheet" type="text/css">
  <link href="/css/default.css" rel="stylesheet" type="text/css">
  <title>The Foods of Southern India</title>
</head>
<body>

<header>The Foods of Southern India</header>

<main>
  <p>
    <%= FeaturedParagraph.new("../dynamic_restaurant_site/config/featured_item.txt").load %>
  </p>

  <dl>
    <% Menu.new(File.expand_path("./config/menu.csv")).items.each do |item| %>
      <dt><%= item.name %> - <%= item.price %></dt><img src="images/<%= item.image %>"></dd>
      <dd><%= item.description %>
    <% end %>
  </dl>
</main>

<footer>
  <address>
    123 Main Street
    Boulder, CO 80301
    <a href="mailto:bob@example.com">Contact Us</a>
  </address>
  &copy; <%= Date.today.year %>
</footer>

</body>
</html>
